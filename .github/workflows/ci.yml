name: CI

on:
  # push:
  # pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        working-directory: jfrog-transfer-automation
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]
      - name: Test
        working-directory: jfrog-transfer-automation
        run: |
          pytest
